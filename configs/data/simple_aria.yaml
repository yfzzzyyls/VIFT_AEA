# @package data

# Simple Aria data configuration for AR/VR training
_target_: src.data.simple_aria_datamodule.SimpleAriaDataModule

batch_size: 16
num_workers: 4
max_train_samples: 1000
max_val_samples: 100